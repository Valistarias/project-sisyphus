import React, { type FC } from 'react';

import './dashboard.scss';

const Dashboard: FC = () => {
  return (
    <div className="dashboard">
      <p>Dashboard</p>
    </div>
  );
};

export default Dashboard;
